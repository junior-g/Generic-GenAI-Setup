<!--
TEMPLATE — copy to structure.md, delete this header block, fill every section.
Generated at activation step S5. Load mode: ALWAYS.

Purpose: so new code lands in the right place without a search, and so the agent knows the module
boundaries it must not cross.

Evidence rule: the tree here is the REAL tree, produced from a depth-limited listing and then verified by
opening representative files. A directory whose purpose you guessed from its name is a guess (F5) — open
something inside it.

Keep it under ~130 lines. Annotate only directories that carry a rule; a comprehensive listing of every
folder is noise that costs context on every request.
-->

# Structure — <project name>

## The tree

Only what an agent needs to place code correctly. Annotate; do not enumerate.

```
<repo>/
├── <dir>/                  <what lives here; the rule that governs it>
│   ├── <dir>/              <...>
│   └── <dir>/              <...>
├── <dir>/                  <...>
└── .ai/                    framework payload, config, and work artefacts
```

## Placement rules

The lookup table for "where does this go". One row per kind of thing the project contains.

| Adding a… | Goes in | Naming | Also update |
|-----------|---------|--------|-------------|
| | | | |

Examples of rows worth having: a screen or page · a shared component · an endpoint or handler · a background
job · a data model or schema · a migration · a type or interface · a constant · a translation key · a test ·
a script.

## Module boundaries

Which layers may depend on which. This is the section that prevents the slow collapse into a ball of mud.

| Layer | May depend on | Must not depend on | Enforced by |
|-------|---------------|--------------------|-------------|
| | | | lint rule / review / nothing |

Where the answer is "nothing", say so. An unenforced boundary is a convention, and conventions are broken
by accident — knowing which ones are only conventions is what makes review worthwhile.

## Entry points

Where execution begins. An agent that cannot find these cannot reason about initialisation order,
configuration loading, or startup failures.

| Entry point | File | Starts |
|-------------|------|--------|
| | `path:line` | |

## Routing / dispatch model

<How a request, command, event, or message reaches its handler. The convention (file-based, decorator-based,
central registry, framework magic), where the map lives, and what an agent must do to register a new one.
If the project has no such layer, say "not applicable" — do not leave the heading blank.>

## Naming conventions

| Thing | Convention | Example |
|-------|-----------|---------|
| Files | | |
| Directories | | |
| Types / classes | | |
| Functions / methods | | |
| Constants | | |
| Tests | | |
| Branches | | |

## Configuration and environment

| | |
|---|---|
| Constants module | `path` — imported, never duplicated |
| Environment accessor | `path` — the only place raw environment is read |
| Config file(s) | `path` |
| Secrets template | `path` |
| Build / tool config | `path` |

## Generated and vendored paths — do not hand-edit

| Path | Generated by | Regenerate with |
|------|--------------|-----------------|
| | | |

Editing generated output is work that disappears at the next build. If you need a change here, change the
generator.

## Where framework artefacts live

| Artefact | Path |
|----------|------|
| Feature / enhancement work | `<artefact_root>/<slug>/**` |
| Bug fixes | `<artefact_root>/hotfixes/<slug>/**` |
| Project rules | `<rules_root>/` |
| Central index | `.ai/INDEX.md` |
| Per-feature steering (transient) | `<rules_root>/8x-<slug>.md` |

## Structural rules that are non-obvious

The traps. Each one is something a reasonable person would get wrong on their first change.

| Rule | Why | Cost of getting it wrong |
|------|-----|--------------------------|
| | | |
